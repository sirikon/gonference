{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/gonference",
            "env": {
                "DATABASE_URL": "postgresql://postgres:12345@localhost/gonference?sslmode=disable",
                "PORT": 3000
            },
            "args": []
        }
    ]
}
