name: Gonference
env:
  DATABASE_URL: "postgresql://postgres:12345@localhost/gonference?sslmode=disable"
  PORT: 3000
  DEVENV_PATH: docker/gonference-devenv
  BACKOFFICE_UI_PATH: src/backoffice-ui
  FRONT_STYLE_PATH: src/front-style
